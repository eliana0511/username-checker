# username-checker
Simple Python program that checks username availability and suggests unique alternatives using a hash table (dictionary).

# 🔐 Unique Username Checker

A simple Python program that checks for username availability and suggests unique alternatives using a **hash table (dictionary)**.

## 💡 Features
- Register new usernames
- Check instantly if a username is taken
- Suggest 3 random unique usernames if unavailable
- View all registered usernames
- Simple and efficient O(1) lookup time

## 🧠 Concept Demonstrated
- **Hash Table (Dictionary)** → Stores and checks usernames instantly
- **Random Module** → Generates fun username suggestions

## 🖥️ How to Run
```bash
python3 username_checker.py

Then commit and push again:
```bash
git add README.md
git commit -m "Add README for Username Checker"
git push
